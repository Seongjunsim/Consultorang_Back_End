<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="login">

    <insert id="insertUser">
        <![CDATA[
            INSERT INTO USER
            (email, pw, phone, business_num, business_cd, business_name,service_yn, reg_dt)
            VALUE(
                #{email},
                #{pw},
                #{phone},
                #{businessNum},
                #{businessCd},
                #{businessName},
                #{serviceYn},
                NOW()
            )
        ]]>
    </insert>

    <select id="getUser" resultType="UserModel">
        <![CDATA[
            SELECT * FROM USER
            WHERE email=#{email}
        ]]>
    </select>

    <select id="checkEmail" resultType="int">
        <![CDATA[
            SELECT
                COUNT(0) CNT
            FROM USER
            WHERE email=#{email}
        ]]>
    </select>
</mapper>